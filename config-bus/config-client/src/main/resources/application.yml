server:
  port: 8001
spring:
  application:
    name: config-client
  profiles:
    active: test
  jackson.serialization.indent_output: true
endpoints:
  sensitive: false
## 刷新时，关闭安全验证
management:
  security:
    enabled: false
# 开启安全验证
security:
  user:
    name: user
    password: password
  basic:
    enabled: true
    path: /**




